# GitHub Personal Access Token（fine-grained）— 用于 vpk 上传 GitHub Releases
#
# 生成：GitHub → Settings → Developer settings → Fine-grained tokens → Generate new token
#   - Repository access: 仅选 MicroDele/CSVHelper
#   - Permissions: Contents → Read and write
#
# 用法：复制本文件为 .env 并填入真实 token。
#   cp .env.example .env
# .env 已被 .gitignore 忽略，绝不会提交到仓库。
#
# 发布命令（在项目根目录的 PowerShell 里）：
#   .\deploy.ps1           # 仅本地构建（publish -> pack -> Releases/）
#   .\deploy.ps1 -Upload   # 本地构建 + 上传 GitHub Releases（读取本 token）

GH_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
